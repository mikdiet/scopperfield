<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :cover %><br />
    <%= f.collection_select :cover, Book::COVER_TYPES, :to_s, :to_s %>
  </div>
  <div class="field">
    <%= f.label :readed %><br />
    <%= f.check_box :readed %>
  </div>
  <div class="field">
    <%= f.label :readed_at %><br />
    <%= f.datetime_select :readed_at %>
  </div>
  <div class="field">
    <%= f.label :pages %><br />
    <%= f.number_field :pages %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
